# Copy to .env and adjust. The app uses PostgreSQL when DATABASE_URL is set.
# Otherwise it keeps the local data/inventory.json file.

DATABASE_URL=postgres://saltbox:saltbox@127.0.0.1:5432/saltbox
SESSION_SECRET=
